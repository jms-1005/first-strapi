'use strict';

/**
 *  newcontenttype controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::newcontenttype.newcontenttype');
